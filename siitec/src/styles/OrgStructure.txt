/* OrgChart.css - Organizational Chart Styling */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');

:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f8fafc;
  --bg-gradient: linear-gradient(135deg, #f0f9ff 0%, #e0e7ff 100%);
  
  /* Color Palette */
  --dean-color: linear-gradient(135deg, #2563eb 0%, #4f46e5 100%);
  --deputy-color: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
  --assistant-color: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
  --department-color: linear-gradient(135deg, #ec4899 0%, #f43f5e 100%);
  --center-color: linear-gradient(135deg, #f43f5e 0%, #ef4444 100%);
  
  /* Border Colors */
  --dean-border: #3b82f6;
  --deputy-border: #818cf8;
  --assistant-border: #a78bfa;
  --department-border: #f472b6;
  --center-border: #fb7185;
  
  /* Background Colors */
  --dean-bg: #dbeafe;
  --deputy-bg: #e0e7ff;
  --assistant-bg: #ede9fe;
  --department-bg: #fce7f3;
  --center-bg: #ffe4e6;
  
  /* Text Colors */
  --text-primary: #1e293b;
  --text-secondary: #475569;
  --text-muted: #64748b;
  --text-white: #ffffff;
  
  /* Spacing & Effects */
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.08);
  --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.12);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.16);
  --shadow-xl: 0 12px 48px rgba(0, 0, 0, 0.2);
  
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  
  --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Container */
.org-chart-container {
  min-height: 100vh;
  background: var(--bg-gradient);
  padding: 2rem 1rem;
}

/* Header */
.org-chart-header {
  max-width: 1600px;
  margin: 0 auto 2rem;
  background: var(--bg-primary);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-lg);
  overflow: hidden;
}

.header-decoration {
  height: 3px;
  background: linear-gradient(90deg, #2563eb 0%, #4f46e5 33%, #8b5cf6 66%, #ec4899 100%);
}

.header-content {
  padding: 3rem 2rem;
  text-align: center;
}

.header-icon {
  width: 64px;
  height: 64px;
  margin: 0 auto 1rem;
  background: var(--dean-color);
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
}

.header-icon svg {
  width: 32px;
  height: 32px;
  color: var(--text-white);
}

.header-title {
  font-size: 3rem;
  font-weight: 800;
  background: linear-gradient(90deg, #2563eb 0%, #4f46e5 33%, #8b5cf6 66%, #ec4899 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1rem;
  letter-spacing: -0.02em;
}

.header-subtitle {
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
}

.header-institution {
  font-size: 1.1rem;
  color: var(--text-secondary);
}

/* Chart Wrapper */
.chart-wrapper {
  max-width: 1600px;
  margin: 0 auto;
  background: var(--bg-primary);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-lg);
  padding: 3rem 2rem;
  overflow-x: auto;
}

.chart-content {
  min-width: 1200px;
}

/* Chart Levels */
.chart-level {
  margin-bottom: 3rem;
  position: relative;
}

/* Level Badges */
.level-badge {
  text-align: center;
  display: inline-block;
  padding: 0.5rem 1.5rem;
  border-radius: 999px;
  font-weight: 600;
  font-size: 0.875rem;
  margin: 0 auto 2rem;
  display: block;
  width: fit-content;
  margin-left: auto;
  margin-right: auto;
}

.deputy-badge {
  background: var(--deputy-bg);
  color: #6366f1;
}

.assistant-badge {
  background: var(--assistant-bg);
  color: #8b5cf6;
}

.department-badge {
  background: var(--department-bg);
  color: #ec4899;
}

.center-badge {
  background: var(--center-bg);
  color: #f43f5e;
}

/* Connectors */
.vertical-connector {
  width: 2px;
  height: 48px;
  background: linear-gradient(180deg, #6366f1 0%, rgba(99, 102, 241, 0.3) 100%);
  margin: 0 auto 2rem;
}

.horizontal-connector {
  position: relative;
  max-width: 1200px;
  margin: 0 auto 2rem;
  height: 48px;
}

.connector-line {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: currentColor;
  opacity: 0.3;
}

.connector-dot {
  position: absolute;
  top: 0;
  width: 2px;
  height: 48px;
  background: currentColor;
  opacity: 0.5;
}

.deputy-connector {
  color: #6366f1;
}

.deputy-connector .dot-1 { left: 12.5%; }
.deputy-connector .dot-2 { left: 37.5%; }
.deputy-connector .dot-3 { left: 62.5%; }
.deputy-connector .dot-4 { left: 87.5%; }

.assistant-connector {
  color: #8b5cf6;
  max-width: 600px;
}

.assistant-connector .dot-1 { left: 25%; }
.assistant-connector .dot-2 { left: 75%; }

.department-connector {
  color: #ec4899;
  max-width: 400px;
}

.department-connector .dot-1 { left: 25%; }
.department-connector .dot-2 { left: 75%; }

.center-connector {
  color: #f43f5e;
  max-width: 500px;
}

.center-connector .dot-1 { left: 16.66%; }
.center-connector .dot-2 { left: 50%; }
.center-connector .dot-3 { left: 83.33%; }

/* Chart Rows */
.chart-row {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
}

.deputy-row {
  max-width: 1200px;
  margin: 0 auto;
}

.assistant-row {
  max-width: 800px;
  margin: 0 auto;
}

.bottom-row {
  gap: 4rem;
}

.chart-section {
  flex: 1;
}

.chart-column {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

/* Chart Nodes */
.chart-node {
  background: var(--bg-primary);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-md);
  cursor: pointer;
  transition: var(--transition);
  position: relative;
}

.chart-node:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-xl);
}

.chart-node.selected {
  transform: scale(1.05);
  box-shadow: var(--shadow-xl);
}

/* Dean Node */
.dean-node {
  max-width: 400px;
  margin: 0 auto;
  background: var(--dean-color);
  border: none;
}

.dean-node .node-content {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1.5rem;
}

.dean-node .node-icon {
  width: 56px;
  height: 56px;
  background: rgba(255, 255, 255, 0.2);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.dean-node .node-icon svg {
  width: 28px;
  height: 28px;
  color: var(--text-white);
}

.dean-node .node-text {
  text-align: left;
  flex: 1;
}

.dean-node .node-label {
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: rgba(255, 255, 255, 0.8);
  margin-bottom: 0.25rem;
}

.dean-node .node-name {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--text-white);
  line-height: 1.3;
}

.node-glow {
  position: absolute;
  inset: -4px;
  background: var(--dean-color);
  border-radius: var(--radius-md);
  filter: blur(12px);
  opacity: 0.25;
  z-index: -1;
}

/* Deputy Nodes */
.deputy-node {
  border: 2px solid var(--deputy-border);
  flex: 1;
  min-width: 0;
}

.deputy-node .node-content {
  padding: 1.25rem;
}

.deputy-node .node-badge {
  width: 32px;
  height: 32px;
  background: var(--deputy-color);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-white);
  font-weight: 700;
  font-size: 0.875rem;
  margin-bottom: 0.75rem;
}

.deputy-node .node-name {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 0.75rem;
  line-height: 1.4;
  white-space: pre-line;
}

.deputy-node .node-tasks {
  list-style: none;
  padding-left: 0;
}

.deputy-node .node-tasks li {
  font-size: 0.8rem;
  color: var(--text-secondary);
  padding: 0.25rem 0;
  padding-left: 1rem;
  position: relative;
  line-height: 1.4;
}

.deputy-node .node-tasks li::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 4px;
  height: 4px;
  background: #6366f1;
  border-radius: 50%;
}

/* Assistant Nodes */
.assistant-node {
  border: 2px solid var(--assistant-border);
  flex: 1;
}

.assistant-node .node-content {
  padding: 1.5rem;
}

.assistant-node .node-badge {
  width: 40px;
  height: 40px;
  background: var(--assistant-color);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-white);
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 0.75rem;
}

.assistant-node .node-name {
  font-size: 1rem;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 1rem;
  line-height: 1.4;
  white-space: pre-line;
}

.assistant-node .node-tasks {
  list-style: none;
  padding-left: 0;
}

.assistant-node .node-tasks li {
  font-size: 0.875rem;
  color: var(--text-secondary);
  padding: 0.35rem 0;
  padding-left: 1rem;
  position: relative;
  line-height: 1.4;
}

.assistant-node .node-tasks li::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 5px;
  height: 5px;
  background: #8b5cf6;
  border-radius: 50%;
}

/* Department Nodes */
.department-node {
  border: 2px solid var(--department-border);
}

.department-node .node-content {
  padding: 1.25rem;
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
}

.department-node .node-icon {
  width: 40px;
  height: 40px;
  background: var(--department-color);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.department-node .node-icon svg {
  width: 20px;
  height: 20px;
  color: var(--text-white);
}

.department-node .node-title {
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
  line-height: 1.3;
  white-space: pre-line;
}

.department-node .node-subtitle {
  font-size: 0.75rem;
  color: var(--text-muted);
}

/* Center Nodes */
.center-node {
  border: 2px solid var(--center-border);
}

.center-node .node-content {
  padding: 1.25rem;
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
}

.center-node .node-icon {
  width: 40px;
  height: 40px;
  background: var(--center-color);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.center-node .node-icon svg {
  width: 20px;
  height: 20px;
  color: var(--text-white);
}

.center-node .node-title-row {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
  flex-wrap: wrap;
}

.center-node .node-title {
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1.3;
  white-space: pre-line;
}

.center-node .node-acronym {
  display: inline-block;
  padding: 0.25rem 0.5rem;
  background: var(--center-bg);
  color: #f43f5e;
  font-size: 0.7rem;
  font-weight: 700;
  border-radius: 4px;
}

.center-node .node-subtitle {
  font-size: 0.75rem;
  color: var(--text-muted);
}

/* Legend */
.chart-legend {
  max-width: 1600px;
  margin: 2rem auto 0;
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  padding: 2rem;
}

.legend-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 1.5rem;
}

.legend-items {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 1rem;
  margin-bottom: 1rem;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.legend-color {
  width: 16px;
  height: 16px;
  border-radius: 4px;
}

.dean-color {
  background: var(--dean-color);
}

.deputy-color {
  background: var(--deputy-color);
}

.assistant-color {
  background: var(--assistant-color);
}

.department-color {
  background: var(--department-color);
}

.center-color {
  background: var(--center-color);
}

.legend-item span {
  font-size: 0.875rem;
  color: var(--text-secondary);
  font-weight: 500;
}

.legend-hint {
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-top: 1rem;
}

/* Footer */
.chart-footer {
  max-width: 1600px;
  margin: 2rem auto 0;
  text-align: center;
  padding: 2rem;
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
}

.chart-footer p {
  font-size: 0.875rem;
  color: var(--text-muted);
}

/* Responsive Design */
@media (max-width: 1400px) {
  .chart-content {
    min-width: 1000px;
  }
}

@media (max-width: 1024px) {
  .header-title {
    font-size: 2.5rem;
  }
  
  .chart-wrapper {
    padding: 2rem 1rem;
  }
}

@media (max-width: 768px) {
  .org-chart-container {
    padding: 1rem 0.5rem;
  }
  
  .header-content {
    padding: 2rem 1rem;
  }
  
  .header-title {
    font-size: 2rem;
  }
  
  .header-subtitle {
    font-size: 1.25rem;
  }
  
  .header-institution {
    font-size: 1rem;
  }
}

/* Accessibility */
.chart-node:focus {
  outline: 3px solid #3b82f6;
  outline-offset: 2px;
}

@media (prefers-reduced-motion: reduce) {
  * {
    animation: none !important;
    transition: none !important;
  }
}